# Prompt Engineering in Action

A practical repository showcasing hands-on techniques and best practices for effective prompt engineering in AI applications.

## References

- [LangChain](https://www.langchain.com/)

## Creating a new Virtual Environment

To create a new virtual environment, you can use the following command:

```powershell
# For Windows PowerShells
python --version

pip install virtualenv
python.exe -m pip install --upgrade pip

python -m venv .venv
.\.venv\Scripts\activate
python.exe -m pip install --upgrade pip

pip install LangChain
pip install langchain-openai
pip install langchain-community
pip install langchain_experimental
```
